<!DOCTYPE html>
<html>
  <!-- <head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head> -->

  <%- include("_head") %> 

  <body>
    
    <%- include("_navbar") %>

   


    <div class="container">
      <div class="row d-flex justify-content-center align-items-center mt-5">
        <div class="col-8 mt-5 ">
          
          <% if (journeys.length === 0) { %>

              <h1>Oops ! No trains available...</h1>
              <a class="btn-danger" href="homepage" value="Home">Home</a>

          <% } else { %>
              
              <h1>Trains available on the XXX</h1>
              <% for (let i=0; i<journeys.length;i++) { %>
                <div class="row ticket-option pt-5 pb-5 px-4 d-flex align-items-center">
                  <div class="col-3 d-flex justify-content-left">
                    <h5><%= journeys[i].departure %> / <%= journeys[i].arrival %></h5>
                  </div>
                  <div class="col-6 d-flex justify-content-center">
                    <h6>Train departure at : <%= journeys[i].departureTime %></h6>
                  </div>
                  <div class="col-1 d-flex justify-content-center">
                    <h3><%= journeys[i].price %>€</h3>
                  </div>
                  <div class="col-2 d-flex justify-content-center">
                    <a class="btn btn-danger" href="order?id=<%= journeys[i].id %>" >OK</a>
                  </div>

                </div>
              <% } %>

          <% } %>
          
        </div>
      </div>



  


    </div>







    <%- include("_scripts") %>   
  </body>
</html>
